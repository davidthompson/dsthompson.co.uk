cloudflare_zone_id = "0123456789abcdef0123456789abcdef"

# Existing records are declared in dns-records.tf. Use this optional map for new
# records or to override an existing entry using the same stable map key.
dns_records = {}
